
This example implements a (not so) simple UI for an IRC client.

Requires luasocket and LuaIRC to be installed in the system.

This example shows how to:

* Use some widgets such as `ListBox`es, `Button`s, and `TextField`s.
* Validate text input.
* Create compound widgets using `Compound`.
